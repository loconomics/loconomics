/* Isolated element mixim.
    A block with a medium margin to separate itself
    from other elements on separated rows
*/
isolated() {
    margin-top: 14px;
    margin-bottom: 14px;
    clear: both;
    display: block;
}
