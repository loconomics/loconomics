/**
    Addresses activity.
    
    It allows for selection of addresses per job title,
    home address or on demand creation of a new (client) address
    to select and use in a booking or event (returning to the 
    requester activity with the selectedAddress).
**/
'use strict';


