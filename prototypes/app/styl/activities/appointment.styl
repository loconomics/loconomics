/** Appointment activity styles */
[data-activity=appointment] {

    &.in-edit {
        
        .CardsDeck-dots,
        .CalendarDateHeader-switch {
            visibility: hidden;
        }
        
        .ActivityTitle-action {
            pointer-events: none;
            visibility: hidden;
        }
    }
}

@import '../components/AppointmentCard.styl'
