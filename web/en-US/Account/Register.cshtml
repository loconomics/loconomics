@{
    // Set the layout page and page title
    Layout = LcUrl.RenderLangPath + "_SiteLayout.cshtml";
    PageData["ShowWelcomePopup"] = true;
    PageData["BodyClass"] = "RegisterPage";
}
@* $WelcomePopup gets included in the SiteLayout because of the setting ShowWelcomePopup=true *@
