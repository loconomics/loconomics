@{
    LcHelpers.TestArea();
    
    var u = LcData.UserInfo.GetUserRow();
    
    if (LcHelpers.InLive && !u.IsAdmin) {
        LcHelpers.RedirectToLogin();
    }

    Session["BookingRequestID"] = Request["BookingRequestID"].AsInt(172);
}
@RenderPage("Confirmation.cshtml")