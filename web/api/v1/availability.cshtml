@*
    [Obsolete="Replaced by /users/{userID}/availablity for QUERIES, STILL IN USE FOR UPDATES"]
    Query the user availability.
    For documentation, read App_Code/RestPages/RestAvailability.cs

    TODO Decouple update actions to its own /me API, now that there is a different public queries API
*@
@{
    if (Request.HttpMethod.ToUpper() == "PUT") {
        Response.RestRequiresUser(LcData.UserInfo.UserType.User);
    }
    new RestAvailability(true).JsonResponse(this);
}
