@{
    DashboardFunctions.SecurePage();
    
    Layout = "_Layout.cshtml";
    Page.Title = "Make your life better!";
    
    PageData["userow"] = DashboardFunctions.GetUserRow();
    PageData["posrows"] = DashboardFunctions.GetUserPos();
    PageData["menu-item"] = "positions";
}

        @{
            int n = 0;
        }
		<ul class="tabs autofill-submenu-items">
            @{
                foreach(var c in PageData["posrows"]){
                    n++;
                    var cls = (n==1 ? "current" : "");
                    <li class="@cls"><a class="@cls" href="@("#position"+n)">Position @n</a></li>
                }
            }
		</ul>
        
        @{
            n = 0;
            foreach(var c in PageData["posrows"]){
                n++;
                var cls = (n==1 ? "current" : "");
        <div id="@("position"+n)" class="tab-body fancy tabbed buttons @cls">
            <ul class="tabs">
                <li><a class="button profile fanciful current" href="@("#position"+n)-services">Services</a></li>
                <li><a class="button profile fanciful" href="@("#position"+n)-pricing">Pricing</a></li>
                <li><a class="button profile fanciful" href="@("#position"+n)-photos">Photos</a></li>
                <li><a class="button profile fanciful" href="@("#position"+n)-locations">Locations</a></li>
                <li><a class="button profile fanciful" href="@("#position"+n)-licenses">Licenses</a></li>
                <li><a class="button profile fanciful" href="@("#position"+n)-reviews">Reviews</a></li>
            </ul>
            
            <div id="@("position"+n)-services" class="tab-body current">
                Services <!-- TODO here must be the back-end content -->
            </div><!-- #@("position"+n)-services -->
            <div id="@("position"+n)-pricing" class="tab-body">
                Pricing <!-- TODO here must be the back-end content -->
            </div><!-- #@("position"+n)-pricing -->
            <div id="@("position"+n)-photos" class="tab-body">
                Photos <!-- TODO here must be the back-end content -->
            </div><!-- #@("position"+n)-photos -->
            <div id="@("position"+n)-locations" class="tab-body">
                Locations <!-- TODO here must be the back-end content -->
            </div><!-- #@("position"+n)-locations -->
            <div id="@("position"+n)-licenses" class="tab-body">
                Licenses <!-- TODO here must be the back-end content -->
            </div><!-- #@("position"+n)-licenses -->
            <div id="@("position"+n)-reviews" class="tab-body">
                Reviews <!-- TODO here must be the back-end content -->
            </div><!-- #@("position"+n)-reviews -->
            
            
            <!-- OLD:: -->
            <hr/>
            <div class="progress-bar">
                <span class="total"><span class="percent-done"></span></span>
                <span class="text"><span class="percent-done">50%</span> complete</span>
            </div>

            <h3 class="job-title">@c.PositionSingular <a class="job-status inactive" href="#">@c.active</a></h3>
            <table class="profile-stats">
                <tr><td>Profile Views:</td><td>0</td></tr>
                <tr><td>Last Updated:</td><td>@c.UpdatedDate.ToString("dd MMM yyyy")</td></tr>
                </table>
                <table class="job-stats">
                <tr><td>Hours booked:</td><td>0</td></tr>
                <tr><td>Lorem Ipsum:</td><td>20</td></tr>
            </table>
            <a href="#">Booking History</a>
            <a href="../PricingProfileWizardPage.cshtml?PositionID=@c.PositionID">Edit Estimate Profile</a>
            <div class="job-actions">
                <a class="button go" href="#">Edit</a>
                <a class="button go" href="#">Publish</a>
            </div>
            <!-- ::OLD -->
            
        </div><!-- #position@n -->
            }
        }
