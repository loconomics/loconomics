@using WebMatrix.Data;
@{
    /* Setup */
    var result = new Dictionary<string, object>();
    // Error result by default
    int resultCode = -1;
    
    try{
        /* Input data */
        var positionId = Request["positionId"].AsInt();
        var filter = Request["filter"];
        
        result.Add("ServiceAttributeCategories", LcData.GetServiceCatsAndItsAttributes(positionId, filter));
        
        // No error
        resultCode = 0;
    }
    catch (Exception ex){
        /* Output error */
        result["ErrorMessage"] = ex.Message;
    }
    
    /* Output data */
    CommonHelpers.ReturnJsonResult(resultCode, result);
}
