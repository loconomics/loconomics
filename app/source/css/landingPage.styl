/*
    Stylesheet for any landingPage.
    Ideally, just generating one file from the landing stylesheet
    would contain any import needed, and not shared with others,
    but for now are few the changes and no much overload
    plus some refactors needed here (maybe just 'app.styl')
*/
/* core */
@import "nib"
@import 'utils/mediaQueries.styl'
@import 'utils/styleguideVars.styl'
@import 'utils/bs-extensions.styl'
@import 'utils/input-group-addon-inset.styl';
@import 'utils/arrows.styl';

/** Utilities styles **/
@import 'utils/isolated.styl'

/** Components styles **/
@import 'components/ActionSheet.styl'
@import 'components/SignupLogin.styl'
@import 'components/LargeNavBar.styl'
@import 'components/Press.styl'
@import 'components/Testimonials.styl'

/** Pages **/
//** e.g., @import 'landingPages/dog-care-professionals.styl' **/

/** App contains some general changes that may be needed**/
/* TODO Ideally this will be shared utilities or components,
 and no forcing the class App on the top */
@import 'components/App.styl'

.App > body {
    // No navbar
    padding-top: 0 !important;
}

.LandingPage {

    // NO NAVBAR, disabled on the JS activity, so this can replace the default:
    top: 0 !important;

    // NOTE: all activities are absolute positioned, so is already a positioning context

    [role=main] {

        .landingpage-brand {
            display: block;
            width: 100%;
            height: 100px;
            background: no-repeat;
            background-image: url('../images/logo-white-large.png');
            background-image: 
              linear-gradient(transparent, transparent),
              url('../images/logo-white.svg');
            background-size: 200px;
            margin: 1.4286rem $grid-gap;
            @media $mediaMaxSm {
                background-position: center;
                margin: 0em; 1.4286em;
                height: 50px;
                background-size: 100px;
            }
        }
        
        .landingpage-copy {
            margin-bottom: 2.1429em;
            padding: 2.1429em; 0em; 2.1429em; 2.1429em;
            background: rgba(0, 152, 154, 0.75); 

            @media $mediaMaxSm {
                margin-bottom: 0;
                margin-top: 0;
                background: none;
            }

            @media $mediaMaxXs {
                padding: 1.0714em;
            }
        }
        .landingpage-copyBackground {
            background: none;            
            @media $mediaMaxSm {
                padding: 1.4286em; 0em; 0em; 0em;
                background: rgba(0, 152, 154, 0.75);   
                margin-top: 0.7143em;
                h1 {
                    margin-top:0px;
                    font-size:21px;
                }
            }                     
        }
        .landingpage-background {
            padding-top: 2.1429em;
            background: no-repeat center / cover;
            background-image:
              url('../images/golden-gate-bridge-388917_1920.jpg');
            -webkit-background-size: cover;
            -moz-background-size: cover;
            -o-background-size: cover;
            background-size: cover;
            @media $mediaMaxSm {
                padding-top: 0em;
            }
          }
        .landingpage-
        .landingpage-title {
            color: white;
            font-size: 2.7143rem;
            font-weight: bold;
            text-align: left;
            @media $mediaMaxSm {
                font-size:  1.7143rem;
            }
        }
        .landingpage-tagline {
            color: white;
            font-size:  1.7143rem;
            text-align: left;
            @media $mediaMaxSm {
                font-size: 1.1429rem;
            }
        }
        .landingpage-proposition {
            color: white;
            font-size: 1.2857rem;
            text-align: left;
            display: block;
            .fa,
            p {
                display: inline-block;
                margin-right: 0.3571em;
                font-size: 1.5714rem;
                @media $mediaMaxSm {
                    font-size: 1.1429rem;
                }
            }
        }


        // Edge limit for phone
        .landingpage-title,
        .landingpage-tagline,
        .landingpage-proposition{
            padding: 0 $grid-gap;
        }
        .SignupLogin {
            h2 {
                text-align: center;
            }

            #job-title-selection {
                margin: 0.7143em;
                button {
                    padding: 0.3571em;
                    margin: 0.1429em;
                    border-width: 0.1429rem;
                    font-weight: 600;
                }
            }
            .LeadContent {
                background: rgba(255, 255, 255, .85);
                margin-top: 0;
            }
        }
        form {
            margin: 0 auto 2.8571em;
            max-width: 450px;

            .form-control {
                background: White;
                font-size: 1.2143rem;
            }

            .form-control:focus {
              border-bottom-color: #ccc;
              border-top-color: transparent;
              border-right-color: transparent;
            }

            // Reduce column gaps by a 1.4286em in total
            // for sizes bigger than XS (so keep standar in phone where
            // forms goes vertical)
            @media $mediaMinSm {
                > * {
                    padding-left: 0.7143em;
                    padding-right: 0.7143em;
                }
            }
        }

        // How the Cooperative Works section
        .landingpage-hiw {
            text-align: center;
            color: white;
            background: $brand-secondary;
            padding-top: 2.8571em;
            $iconsSize = 90px;
            .landingpage-title {
              color: white;
              text-align: center;
            }
            ul {
                margin-top: ($iconsSize / 2) + 1.2857rem;
                margin-bottom: 0;
                padding-bottom: ($iconsSize / 2);

                @media (max-width: 520px) {
                    margin-top: 0em;
                }
                > li {
                  margin-top: (0 - $iconsSize / 2);
                  @media (max-width: 520px) {
                    margin-top: 2.1429em;
                  }
                  @media $mediaMaxXs {
                      margin-top: 0em;
                  }
                }
            }
            @media (max-width: 520px) {
                    margin: 0 $grid-gap;
            }
            h3 {
                font-weight: bold;
                font-size: 1.5714rem;
            }
            // icons
            i {
                background: $brand-secondary;
                border: 0.1429rem solid white;
                font-size: floor($iconsSize / 1.60);
                border-radius: 50%;
                width: $iconsSize;
                height: $iconsSize;
                line-height: $iconsSize;
                color: white;
            }
        }
    }
}

