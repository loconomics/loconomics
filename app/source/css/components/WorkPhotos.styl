/**
    WorkPhotos component for the serviceProfessional photos dahsboard
**/

.WorkPhotos {
    max-width: 442px;
    display: block;
    width: auto
    overflow: hidden;
    // For 'sortable' reordering
    position: relative;
    
    > li {
        position: relative;
        margin-bottom: 15px;

        .WorkPhotos-imgBtn {
            display: block;
            position: relative;

            img {
                display:block;
                width: auto;
                height:auto;
                max-width:100%;
                max-height:332px;
            }

            .WorkPhotos-editionLayer {
                position: absolute;
                top: 0;
                right: 0;
                bottom: 0;
                left: 0;
                background: rgba(255, 255, 255, 0.6);
                pointer-events: none;
                opacity: 0;
                transition: opacity 300ms ease;
                
                // IMPORTANT: COMPUTED .btn class heigh
                $btnLgHeight = 46px;
                > .WorkPhotos-toolbar {
                    width: 100%;
                    height: 100%;
                    display: block;
                    text-align: center;
                    // Vertically aligning, knowing the button height and for
                    // a single row of buttons
                    position: absolute;
                    top: 50%;
                    margin: (0 - $btnLgHeight / 2) auto 0;
                }
            }
        }
        
        &.is-selected {
            .WorkPhotos-editionLayer {
                //display: block;
                opacity: 1;
                pointer-events: auto;
            }
        }
    }
}
