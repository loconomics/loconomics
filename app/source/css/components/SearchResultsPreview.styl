/**
    Search Results Preview component.
    Created to be uses as the list of results inside an autocomplete dropdown.
 */
 // TODO Better naming
.SearchResultsPreview {
    background-color: white;
    display: block;

    ul {
        padding: 0;
        margin-bottom: 0;

        li {
            list-style: none;
        }
    }

    .SearchResultsPreview-h {
        padding-top: 5px;
        font-size: 18px;
        text-align: center;
        margin: 10px 0;
        color: $gray-dark;
    }
}
