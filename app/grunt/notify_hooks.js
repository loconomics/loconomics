'use strict';

module.exports = {
    notify_hooks: {
        options: {
            enabled: true,
            max_eslint_notifications: 5 // maximum number of notifications from eslint output
        }
    }
};