@{


        this.Validation.RequireField("username", "[[[You must specify a user name.]]]");
        this.Validation.RequireField("password", "[[[You must specify a password.]]]");

        if (!this.Validation.IsValid() && 1 == 1)
        {
            // Bad request, input data incorrect because of validation rules
            //throw new HttpException(400, "Please fix these issues and try again:");

        }

        this.Response.ContentType = "application/json";
        this.Response.Write(@"
            {
                ""errorMessage"": ""Please fix these issues and try again: "",
                ""errorSource"": ""validation"",
                ""errors"": {
                        ""password"": [
                                ""[[[You must specify a password.]]]""
                    ]
            }
            }
        ");

    //this.Response.End();

}